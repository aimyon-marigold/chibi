w = int（input（））
s = [範囲内のiのi（w + 1）]
n = int（input（））
範囲（n）のiの場合：
    a = list（map（int、input（）。split（ '、'）））
    s [a [0]]、s [a [1]] = s [a [1]]、s [a [0]]

範囲（1、w + 1）のiの場合：
    print（s [i]）
